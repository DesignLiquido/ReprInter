// primeira forma alias
importar interpretador

declarar principal vazio {
    alocar tabela Tabela = Tabela([], 0)
    alocar codigo texto = "1 + 1\n"
    alocar tamanho inteiro = @tamanho_texto(codigo)
    escrevaln(interpretador(tabela, codigo, tamanho))
}
