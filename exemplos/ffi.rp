externo {
    declarar escreva(...) vazio;
}

declarar valor_padrao(x inteiro = 9, y inteiro = 60) inteiro {
    retorne x + y
}

declarar principal vazio {
    escreva(valor_padrao(), "\n")
    fim
}
