externo {
    declarar escrevaf(msg texto, ...) vazio;
    declarar entrada(msg texto = "") texto;
}

declarar principal vazio {
    alocar nome texto = entrada("Nome: ")
    escrevaf("Olá %s\n", nome)
    fim
}
