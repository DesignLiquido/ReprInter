// constantes são poderosas
// constantes devem ser declaradas fora de funções pois vão ter o mesmo efeito

constante VERSAO texto = "0.1.0"

declarar mostrarVersao vazio {
    @escreva(VERSAO, "\n")
}

declarar principal vazio {
    mostrarVersao()
    fim
}
