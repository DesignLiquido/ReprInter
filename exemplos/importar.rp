importar pasta.outra.exportar : { soma_publica, mostrar_pi, escrevaln, Status, Pagamento, loop }
// importa tudo o que é publico
//importar "pasta/outra/exportar.rp"

const PI f64 = 100

declarar principal vazio {
    alocar pagamento Pagamento = Pagamento(Status.Pendente)
    escrevaln(soma_publica(10, 10))
    mostrar_pi() // privado
    escrevaln("PI local: ", PI) // local
    //loop()
    fim
}
