declarar fibonacci(n int) int {
    se n <= 1 retorne n
    retorne fibonacci(n - 1) + fibonacci(n - 2)
}

declarar principal vazio {
    __nucleo_harpy_escreva("Fibonacci(10) = ", fibonacci(10), "\n")
    fim
}
