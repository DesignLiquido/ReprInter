// funciona tanto com o nome curto quanto com o nome completo
// fn = funcao
// ime = imediato
// ret = retorne

fn i32 main() {
    aloca i32, $a  // cria uma variavel
    ime i32, $a, 16 // armazena um valor imediato
    ret $a
}
